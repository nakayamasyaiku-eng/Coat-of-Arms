import {getTranslations, setRequestLocale} from 'next-intl/server';
import type {Locale} from '@/i18n/routing';
import {MuseumFrame} from '@/components/MuseumFrame';

export default async function CraftPage({params}:{params:Promise<{locale:Locale}>}){const {locale}=await params;setRequestLocale(locale);const t=await getTranslations('Craft');const s=await getTranslations('CraftSteps');return <section className="page-section"><header className="section-shell page-hero"><p className="kicker">{t('kicker')}</p><h1>{t('title')}</h1><p>{t('intro')}</p></header><div className="section-shell craft-editorial"><MuseumFrame src="/images/framed/kutna-hora.png" alt={locale==='zh'?'库特纳霍拉铜板拓印装裱作品':'Framed Kutná Hora copper impression'} priority/><div className="craft-timeline">{['one','two','three','four'].map((key,i)=><article key={key}><span>0{i+1}</span><div><h2>{s(`${key}Title`)}</h2><p>{s(`${key}Text`)}</p></div></article>)}</div></div><div className="story-band"><div className="section-shell story-grid"><div className="story-monogram copper" aria-hidden="true">CA</div><div><h2>{t('traceTitle')}</h2><p>{t('traceText')}</p><h3>{t('certificateTitle')}</h3><p>{t('certificateText')}</p></div></div></div></section>}
